# Coach analysis notebooks

Run notebooks in numeric order. They use existing processed defensive-action data and OOF predictions when present, and write derived review artefacts under `outputs/coach_analysis/`.

The notebooks are coach-facing: they separate observed outcomes, model predictions, tactical proxy labels and unmeasured hypotheses. They do not retrain models.
