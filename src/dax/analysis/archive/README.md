# Archived (dead) analysis code

Soft-deleted on 2026-09-16 -- confirmed zero imports from any current entry
point (`scripts/analyze_*.py`, `scripts/build_*.py`, `scripts/run_player_clustering.py`,
`scripts/generate_*.py`) or from the test suite. Kept here instead of hard-deleted
in case something surfaces that still needed it.

- `exposure.py` -- no importer anywhere.
- `target_analysis.py` -- no importer anywhere.
