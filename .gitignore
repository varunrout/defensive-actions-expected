# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
.pytest_cache/
.mypy_cache/
.coverage*
htmlcov/

# Environments
.venv/
venv/

# IDE / editor
.idea/
.vscode/

# Jupyter
.ipynb_checkpoints/

# Logs
*.log
pipeline.log

# Local data artifacts
/data/raw/**
!/data/raw/.gitkeep
/data/processed/**
!/data/processed/.gitkeep
/data/features/**
!/data/features/.gitkeep
/data/models/**
!/data/models/.gitkeep
/data/validation/**
!/data/validation/.gitkeep

# OS files
.DS_Store
Thumbs.db


# Generated outputs (preserve archive warnings and directory placeholders)
/outputs/models/**
!/outputs/models/.gitkeep
# The canonical, frozen match-grouped split (prompt 13) is a versioned
# artifact, not a throwaway model output -- it must be checked in so every
# clone/environment shares the exact same match assignment across the
# active and passive legs, rather than each falling back to its own random
# split. Everything else under outputs/models/splits/ stays ignored.
!/outputs/models/splits/
/outputs/models/splits/*
!/outputs/models/splits/match_assignment.json
/outputs/metrics/**
!/outputs/metrics/.gitkeep
/outputs/figures/**
!/outputs/figures/.gitkeep
/outputs/reports/**
!/outputs/reports/.gitkeep
/outputs/validation/**
!/outputs/validation/README.md
/outputs/archive/pre_methodology_fix/**
!/outputs/archive/pre_methodology_fix/README.md
/outputs/features/**
!/outputs/features/.gitkeep
local_archive/
mlruns/
mlartifacts/
outputs/oof/*.parquet
outputs/**/*.parquet
outputs/models/**/*.joblib
mlflow.db
mlflow.db-journal
mlflow.db-shm
mlflow.db-wal
