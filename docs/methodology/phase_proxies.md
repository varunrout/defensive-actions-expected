# Defensive phase proxies

Phase labels are rule-based proxies. Output rows include `phase_source`, `phase_confidence`, and `phase_rule_id`. Counterpress and transition labels are stateful across match/period possession changes. Settled labels are not manually validated tactical truth.
