# 360 visibility handling

Freeze-frame counts are visibility-conditioned. Low visible defender counts must not be interpreted as actual low defender counts unless the relevant local region is visible. Current code keeps visibility fields as controls/placeholders; full Shapely-based polygon coverage computation remains future work before strong claims about player availability.
