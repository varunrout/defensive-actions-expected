# Validation protocol

Use grouped validation by match for baseline model checks. Active validation commands are exposed through `scripts/validate_models.py`. Historical validation outputs are archived and are not current results.
